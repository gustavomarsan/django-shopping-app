# Core
Django==4.1.5
Pillow==11.0.0            # ImageField support (Article.photo, Seller.photo)
python-dotenv==1.2.1      # .env loading in shop/settings.py

# HTTP clients used by shopapp/services/
aiohttp==3.13.3           # async calls to Open-Meteo (weather_service)
requests==2.32.5          # ipapi.co geolocation (location_service)
certifi==2026.2.25        # SSL context for aiohttp
